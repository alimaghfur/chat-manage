{
  "name": "wa-api-gateway",
  "version": "2.0.0",
  "description": "WhatsApp API Gateway - Multi-session REST API with Dashboard",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:dashboard\"",
    "dev:api": "cd api && npm run dev",
    "dev:dashboard": "cd dashboard && npm run dev",
    "install:all": "cd api && npm install && cd ../dashboard && npm install",
    "build": "cd api && npm run build && cd ../dashboard && npm run build",
    "start": "cd api && npm start",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
